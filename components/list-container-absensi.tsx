import React from 'react';

const ListContainerAbsensi = ({ className }: { className?: string }) => {
	return <ul className={className}>ListContainerAbsensi</ul>;
};

export default ListContainerAbsensi;
